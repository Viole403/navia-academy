// Safe test-only env — always uses localhost defaults
export const env = {
  apiBaseUrl: "http://localhost:8080/api/v1",
  mediaBaseUrl: "http://localhost:9000",
  sentryDsn: undefined,
  apiDebug: false,
}
