{
  "name": "@navia/utils",
  "version": "1.0.0",
  "private": true,
  "description": "Shared utility functions for Navia Academy",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "lint": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "clsx": "^2.1.1",
    "tailwind-merge": "^3.6.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
